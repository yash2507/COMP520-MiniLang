# Scott Cooper
# 260503452
# COMP520 - Winter 2016
#
# Test a bunch of if expression parsing as well as make sure that nested if statements are handled as expected
var x : int;
var y : float;
var z : string;

x = 1000000;
y = .123;
z = "hello Minilang";

if -(1 - -2) then
    print "Unary minus worked with paranthesis";
else 
    if -.123 then
        print "unary minus on a float worked";
    else 
        while x do
            x = x + -x;
        done 
    endif 
endif

