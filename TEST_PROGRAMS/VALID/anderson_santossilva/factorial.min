# Computes factorial(n). 
# Read n from the standard input.
# Produce output in the format: "Factorial of n is RESULT.".

# Variable declarations.
var n: int;
var res: int;

# Prints the menu and reads n.
print "Enter n greater than 0.";
read n;

# Sets the default result to 0 and calculates the factorial.
res = 1;
while n do		
	res = n * res;
	n = n - 1;
done

# Print the output.
print "factorial of ";
print n;
print " is ";
print res;
print " .";

